{
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1"
  },
  "scripts": {
    "start": "node index.js",
    "heroku-postbuild": "cd friends && npm install --only=dev && npm install && npm run build"
  }
}
